<%= form_for(topi) do |f| %>
  <% if topi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topi.errors.count, "error") %> prohibited this topi from being saved:</h2>

      <ul>
      <% topi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
